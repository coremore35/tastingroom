<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>New Boardgame</title>
  </head>
  <body>
    <h1>EDIT</h1>

    <a href="/boardgames"><button>Back</button></a>

    <form
      action="/boardgames/<%=game.id%>?_method=PUT"
      method="POST"
      id="usrForm"
    >
      <label for="name">Name: </label>
      <input type="text" name="name" value="<%=game.name %>" />

      <label for="own">Own: </label>
      <input type="checkbox" name="own" <% if(game.own === true){ %> checked <%
      } %> />

      <label for="wishList">Wish List: </label>
      <input type="checkbox" name="wishList" <% if(game.wishList === true){ %>
      checked <% } %> />

      <label for="myRating">My Rating: </label>
      <input
        type="number"
        name="myRating"
        value="<%= game.myRating || 0%>"
        min="1"
        max="10"
      />

      <label for="gamePlayNotes">Game Play Notes: </label>
      <textarea name="gamePlayNotes" form="usrForm">
<%=game.gamePlayNotes%></textarea
      >

      <input type="submit" name="submit" value="submit" />
    </form>
  </body>
</html>
